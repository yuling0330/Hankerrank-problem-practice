def countingValleys(n, s):
    res=0
    vally =0
    for i in range(0,n):
        if s[i] == "U":
            res += 1
            if res== 0:
                vally +=1
        if s[i] == "D":
            res -= 1

        #print(res)
    return vally
